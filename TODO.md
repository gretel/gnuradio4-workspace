# Upstream issues requiring changes

Issues across the GNU Radio 4 ecosystem that need upstream fixes to `gnuradio4`, `gnuradio4-blocks`, or `gnuradio4-dev` (superbuild). Covers macOS, Linux, and Windows platforms.

| # | Issue | Repo | File(s) | Platforms | Symptom | Fix needed | Status |
|---|-------|------|---------|-----------|---------|------------|--------|
| 1 | **`gnuradio4::gr-libsoundio` target missing** | `gnuradio4-blocks` | `blocks/audio/CMakeLists.txt` | macOS, Linux | `CMake Error: target "gr-audio" contains gnuradio4::gr-libsoundio but target not found` | Stop relying on core's exported target — add `find_path`/`find_library` + create `IMPORTED INTERFACE` target locally in blocks | ✅ Fixed locally in `gnuradio4-blocks` checkout; no PR |
| 2 | **`bm_Scheduler.cpp` UDL without namespace** | `gnuradio4` | `bench/bm_Scheduler.cpp` | macOS, Linux (ci config) | Build error: `"out"s` / `"in"s` user-defined literal requires `using namespace std::string_literals;` | Add `using namespace std::string_literals;` | ❌ No upstream fix; workaround: pin gnuradio4 commit or add CMake patch |
| 3 | **`-ldl` linker flag on Windows/MinGW** | `gnuradio4` | `CMakeLists.txt` or `core/src/CMakeLists.txt` | Windows (LLVM MinGW) | Link failure: `-ldl` doesn't exist on Windows | Wrap `-ldl` with `if(NOT WIN32)` or use `CMAKE_DL_LIBS` | ❌ Superbuild mitigates via `CMAKE_DL_LIBS=""`; upstream CMakeLists.txt still emits `-ldl` for the `gnuradio4` exe target |
| 4 | **`thread_affinity.hpp` MinGW native_handle_type** | `gnuradio4` | `core/include/gnuradio-4.0/thread/thread_affinity.hpp` | Windows (MinGW) | Compile error: `native_handle()` returns `void*` not `pthread_t` on MinGW → breaks `native_handle_type` template | Add MinGW detection (`__MINGW32__`) and guard POSIX-only code sections with `!defined(__MINGW32__)` | ❌ No upstream fix; Windows patch exists on `gretel/gnuradio4:ci/windows-build` |
| 5 | **`BlockRegistry.hpp` dllexport redeclaration** | `gnuradio4` | `core/include/gnuradio-4.0/BlockRegistry.hpp` | Windows (MinGW) | Warning: `-Wdll-attribute-on-redeclaration` for `globalBlockRegistry` / `globalSchedulerRegistry` | Guard `__declspec(dllexport)` with Windows-only conditional, or restructure declaration order | ❌ Harmless warning; no upstream fix |
| 6 | **Circular symbol deps — `LINK_GROUP:RESCAN`** | `gnuradio4` | `CMakeLists.txt` | Linux | Linker errors from circular dependency between `gnuradio-blocklib-core` and `gnuradio-core` | Add `$<LINK_GROUP:RESCAN,gnuradio-blocklib-core gnuradio-core>` — macOS `ld64` rescans by default | ❌ Not yet upstreamed |
| 7 | **macOS `-flat_namespace` / `-dead_strip` vs `--gc-sections`** | `gnuradio4` | `CMakeLists.txt` | macOS | BlockRegistry fails without `-flat_namespace`; `--gc-sections` not supported on macOS (needs `-dead_strip`) | Add platform-conditional linker flags in upstream CMakeLists.txt | ❌ Superbuild handles this in `_EP_COMMON_ARGS`; upstream repo lacks the guard |
| 8 | **macOS `-stdlib=libc++` not forced upstream** | `gnuradio4` | `CMakeLists.txt` | macOS (Homebrew LLVM) | Build uses system libc++ instead of LLVM's; ABI mismatch possible | Add `if(APPLE AND CMAKE_CXX_COMPILER_ID MATCHES "Clang")` → `-stdlib=libc++` in upstream CMakeLists.txt | ❌ Superbuild forces it; upstream doesn't |
| 9 | **`ci_defconfig` enables `ENABLE_AUDIO=y`** | `gnuradio4-dev` | `configs/ci_defconfig` | macOS, Linux | `ci` profile tries to build audio, hits issue #1 (missing target) | Set `ENABLE_AUDIO=n` until fix #1 is upstreamed, or apply the local fix first | ⚠️ Workaround: local fix #1 resolves this; ci_defconfig still needs update |
| 10 | **Missing Windows CMake presets** | `gnuradio4` | `CMakePresets.json` | Windows | No `--preset windows` defined | Add `windows` presets (dev, ci) — clang++/Ninja/MinGW, `GR_USE_FETCHCONTENT_DEPS=ON`, block registry OFF | ❌ Presets exist on `gretel/gnuradio4:ci/windows-build`; not upstream |
| 11 | **`GIT_REV` baked at configure time** | `gnuradio4-dev` (superbuild) | `CMakeLists.txt` | all | `GIT_REV` not updated on rebuild; used as subproject version string | Add `git rev-parse` logic to rebuild step, not just configure | ❌ Cosmetic; documented in AGENTS.md |
| 12 | **`gnuradio4-algorithm` → `gnuradio4-library` rename** | `gnuradio4` | `CMakeLists.txt` (target name) | all | Repo renamed to `gnuradio4-library`, config target `gnuradio4Library` | Superbuild updated to `GIT_REPO gnuradio4-library`, workspace uses `find_package(gnuradio4Library)` | ✅ Done upstream + local update |
| 13 | **cpp-httplib unconditional dep being removed upstream** | `gnuradio4` | `core/CMakeLists.txt` | all | cpp-httplib is an unconditional FetchContent dep in core; upstream removing it | Wait for upstream removal; Windows stub (httplib-stub) already in place | ❌ Upstream in progress; no ETA |

**Key:**
- ✅ = fixed locally
- ❌ = not yet upstreamed
- ⚠️ = workaround exists, needs config update
